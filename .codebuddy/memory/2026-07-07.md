## 2026-07-07

- 用户反馈 HANDOFF.md 描述的地形系统不稳定，截图显示大量石柱/尖刺。
- 根因：`CellGenerator.computeLand()` 中 `landDetail` 和 `landRidge` 虽然声明了 `baseFreq=1/800`、`ridgeFreq=1/250`，但实际调用 `Simplex.compute(worldX, worldZ)` 时未乘以频率，导致 Simplex 以原始块坐标采样，每个 block 几乎独立随机，形成密集石柱。
- 已修复：在 `CellGenerator` 中新增 `detailFreq` 和 `ridgeFreq` 字段，并在 `computeLand` 中对 `landDetail`/`landRidge` 的采样坐标做频率缩放。
- 额外发现：当前 `Simplex` 实现（`scaler=99.869484f`）实测输出范围约 `[-1.42, 1.42]`，超过 `Noise.minValue/maxValue` 声明的 `[-1, 1]`。若后续陆地高度仍偏高，应把 scaler 校准到约 `70.0f` 或调低 land detail/ridge 权重。

## 重构计划 v3.0 定稿（PLAN.md 已重写）

- 通读 `参考/sources/SimpleHydrology`（water.h/world.h/vegetation.h + 差异报告），确认其定位为「单块粒子水力侵蚀」，适合做侵蚀系统的水通道；宏观河网仍用 ReTerraForged rivermap。
- 用户确认决策：① 河流/流量用 rivermap 程序化网络（放弃 FastFlow）；② 主参考源 = ReTerraForged+TerraForged 0.3.x+RTG+SimpleHydrology；③ 侵蚀权重 Config 自由调（默认50/30/20）；④ SimpleHydrology 以 C++ 原版为准忠实移植为水通道。
- 已整体重写 PLAN.md → v3.0：P3 改为 rivermap 河网，P4 改为水/风/热/冰归一化混合（水=SimpleHydrology 移植），并附移植红线（禁用坏掉的 Java 移植版、C++ 原版参数、确定性哈希）。

## PLAN.md 高度模型完善（vanilla-faithful）

- 用户要求噪声地形遵循原版设定：海平面高度、海洋最低深度、山体最高高度；海洋分深海/浅海等。
- 已重写 PLAN.md §4「地形高度模型（vanilla-faithful）」：原版基准常量(MIN_Y=-64/MAX_Y=320/SEA_LEVEL=63/BASE_HEIGHT=128/MOUNTAIN_CAP=230/VERTICAL_SCALE=1.0)；地形分层表(深海≈18/浅海≈43/海滩/低地/丘陵/山地/雪峰)；海洋深度 Config(SHALLOW_OCEAN_DEPTH=20/DEEP_OCEAN_DEPTH=45/TRENCH_DEPTH=60)；海洋类型分带(深度×纬度温度→deep_*/lukewarm/warm/cold/frozen)；雪线 SNOW_LINE=140；河流入海钳制(河谷不得切到海平面下)。
- 同步扩展 `GeoGenesisConfig.java`：新增 MOUNTAIN_CAP / VERTICAL_SCALE / SHALLOW_OCEAN_DEPTH / DEEP_OCEAN_DEPTH / TRENCH_DEPTH / SNOW_LINE 六个 ConfigValue。
- §6 补充边界：启用自定义 cave/structure/ore 时必须禁用原版对应生成，避免叠加。
- 待用户确认的其他补充项（暂未写入）：内陆湖泊(lake mask)、安全出生点(陆地)、基岩层细节、HeightCurve 噪声阈值默认值、VERTICAL_SCALE 与侵蚀/河网的联动。

## PLAN.md 四项补充已全部写入

用户确认把四项全部写进 PLAN.md：① 内陆湖泊(lakeMask/lakeLevel + LakeGenerator，闭流洼地填淡水，P3.9) + 安全出生点(findSpawn 螺旋搜索陆地缓坡)；② VERTICAL_SCALE 联动规则(只在 HeightCurve 陆上分支生效、海深不受影响、侵蚀/河网同坐标系自动缩放)；③ 禁用原版特性清单(Generator 显式关 carver/ore/structure/surface/spring-lake/biome装饰)；④ 基岩层细节(BedrockColumn 式随机带) + HeightCurve 噪声阈值默认值(DEEP_THRESHOLD=-0.35/BEACH_BAND=0.04/HILL=0.30/MTN=0.55/PEAK=0.80)。已同步更新 Cell 字段、river/ 包结构、P3 阶段步骤、Generator 架构说明。

## PLAN.md 全面审查（真实地形/现实河流视角）

用户要求按"真实地形模组"标准全面审查计划书。发现并修复的真实感短板，写入 PLAN.md §4「现实感强化」小节（R1–R8）+ 修正两处不一致：
- 不一致修复：陆地高度锚点原与 HeightCurve 分层表矛盾（BASE_HEIGHT=128 vs 海面锚定），改为"陆地以海平面为锚"，VERTICAL_SCALE 改为乘陆地起伏振幅；同步改 GeoGenesisConfig 注释。
- R1 大陆-高度耦合：大陆噪声即地形场低频分量，海岸线=海平面穿越，不再独立二值掩码。
- R2 海洋大陆架剖面：深度随离岸递增（shelf→slope→abyssal），非深/浅两档。
- R3 造山：ridged multifractal 成山脉 + 造山带加权（陆地内部/边缘，海岸衰减）。
- R4 河流现实性：排水一致(沿下坡、不横切山脊、汇入海/湖) + 河网密度∝降水 + 河谷形态(V/U+蜿蜒+泛滥平原) + 河口三角洲。
- R5 侵蚀权重改为气候驱动（全局50/30/20仅兜底缺省）。
- R6 沉积：冲积平原+三角洲；R7 水平地质层被地形剥露；R8 操作顺序：侵蚀→河流下切→湖泊→表面，侵蚀跳过 riverMask。

## 河流现实性 R4 最终决策

用户授权"你决定"。决定采用 **rivermap 先生成连续河网 + 河谷吸附到已侵蚀地形最低下坡路径（tile 带 border 局部 D8 追踪，跨 tile 连续）** 方案，保留已确认 rivermap 选型、零边界断裂；**flow-accumulation 仅作可选后期增强，不纳入基础架构**。

## P0 骨架详细方案已写入 PLAN.md

用户说"你决定"后，将 P0 从一句话步骤扩写为可落地详细实现方案：P0.0 工程改名(mod_id=geogenesis) → P0.1 GeoGenesisMod 注册 CHUNK_GENERATOR/BIOME_SOURCE CODEC → P0.2 GeoGenesisConfig(已写) → P0.3 GeoLevels record → P0.4 GeoGenesisBiomeSource(返平原) → P0.5 GeoGenesisGenerator(fillFromNoise 平坦) → P0.6 注册 WorldPreset 让世界可选 → P0.7 预览窗口占位 → P0.8 编译进游戏验证。按"先案后码"规则，方案写好后需用户确认再编码。

## P0 实现完成（已编译+打包成功）

- 用户说"开始"后编码实现 P0。`.\gradlew.bat build --offline` 通过（EXITCODE=0），产物 `build/libs/geogenesis-1.0.0.jar`。
- 关键修正（Forge 1.20.1-47.4.10 真实 API，与最初猜测不同）：
  - `ChunkGenerator` 构造器是 **2 参** `super(biomeSource, Function<Holder<Biome>,BiomeGenerationSettings>)`，**没有** `Optional<Holder<DimensionType>>` 3 参版本。
  - `WorldGenRegion` 在 `net.minecraft.server.level.WorldGenRegion`（**不是** `net.minecraft.world.level`）。
  - `NoiseColumn` / `StructureManager` 在 `net.minecraft.world.level`；`LevelStem` 在 `net.minecraft.world.level.dimension`。
  - `applyCarvers` 签名：`(WorldGenRegion, long, RandomState, BiomeManager, StructureManager, ChunkAccess, GenerationStep.Carving)`（多了 `BiomeManager` 参数，无 `CarvingContext`/`Aquifer`）。
  - `createStructures` 签名：`(RegistryAccess, ChunkGeneratorStructureState, StructureManager, ChunkAccess, StructureTemplateManager)`（**不是** `ChunkGenerator`+`LevelStem`）。
  - **`findNearestMapFeature` 不存在**，1.20.1 改名叫 `findNearestMapStructure`（非 abstract），P0 直接不覆写。
  - `StructureFeature` 类名已不存在，宏河流/结构相关用 `net.minecraft.world.level.levelgen.structure.Structure`。
  - `BiomeSource` 抽象方法为 `collectPossibleBiomes(): Stream<Holder<Biome>>`（**不是** `possibleBiomes()`）；`getNoiseBiome` 为 `public`。
  - **`BuiltInRegistries` 没有 `BIOME` 静态字段**（此 Forge 构建特有）；固定生物群系改用 vanilla `FixedBiomeSource` 模式：BiomeSource 用 codec 持有 `Holder<Biome>`（`Biome.CODEC` 类型为 `Codec<Holder<Biome>>`），在 world_preset JSON 里用 `"biome": "minecraft:plains"` 提供。
  - `ResourceLocation(String,String)` 构造器被标 `@Deprecated(forRemoval=true)` 且编译报错；改用单参 `new ResourceLocation("geogenesis:generator")`。
  - 世界预设走**数据包**而非 Java WorldPreset API：`src/main/resources/data/geogenesis/worldgen/world_preset/geogenesis.json`，overworld 引用 `geogenesis:generator` + `geogenesis:biomesource`。
- 已沉淀为长期记忆（见 MEMORY.md「Forge 1.20.1 API 关键签名」）。

## P0 修复（世界预设不显示问题）

用户反馈"没有世界可选"。两个根因：
1. **项目根 `data/` 目录**含 `data/minecraft/tags/worldgen/world_preset/normal.json`（只列 `geogenesis:geogenesis`），**替换了 vanilla 的 normal 标签** → vanilla 预设全隐。若我们的预设再解析失败 → 列表完全为空。
2. CODEC 注册方式从 `RegisterEvent` 改为 `DeferredRegister`（Forge 1.20.1 标准方式）。
3. 世界预设补全 `minecraft:the_nether` + `minecraft:the_end` 维度（使用 vanilla noise generator），确保 `WorldPreset` 完整可解析。
4. 删除残留旧代码 `worldgen/terrain/CellGenerator.java` 及其引用。

修复后 `.\gradlew.bat build --offline` 通过。jar 中已无 `data/minecraft/tags/` 覆盖，预设走 `src/main/resources/data/geogenesis/worldgen/world_preset/geogenesis.json`（含 overworld+nether+end）。

## P0 最终验证通过（06:27）

用户跑 `runClient --offline` 后确认：**创建世界界面出现 GeoGenesis 世界类型**，进入后地形为**平坦层（像超平坦世界）**——这正是 P0 的预期行为（骨架只做平坦地形 + 管线/注册验证，真实地形在 P1–P9）。
- 关键修复即：把 `geogenesis:geogenesis` 加入 `data/minecraft/tags/worldgen/world_preset/normal.json`（保留全部 vanilla 预设）。
- **P0 全部交付项达成**：世界可选、进游戏出平坦层、Config 生效、包结构立起、`build` 出 jar。
- 下一步按 PLAN 进入 **P1（噪声树 NoiseEngine/NoiseSampler）**。

## P1 实现完成（06:49，build 通过 EXITCODE=0）

用户选 **B 早验证 + R1 雏形**。已交付：
- **零 MC 依赖噪声树**（包 `worldgen/noise/`）：接口 `Noise`/`Seeded`/`NoiseUtil`/`Seed`/`Noises`（DIRECT_CODEC 注册表，预留 P9 数据包）；基础 `Constant`/`White`；梯度 `Simplex`(忠实 2D,scaler=70)/`Ridge`/`Billow`；运算 `Add`/`Multiply`/`Min`/`Max`/`Abs`/`Invert`/`Power`/`Clamp`；变换 `Frequency`/`Blend`/`Boost`/`Cache2d`/`Warp`/`Map`/`Curve`/`Terrace`/`Steps`。共 23 个模块。
- **TerrainNoise**（`worldgen/noise/terrain/`，R1 海陆）：大陆低频分形 + 域扭曲(Warp) → 自然海岸线；陆地掩码(`Map`+`Clamp`)；脊线山脉分形仅陆地出现；细节高频小振幅；组合为连续高度标量 `e∈[-1,1]`，线性映射到 Y（e≥0 陆上、e<0 海洋，海平面处海岸线连续无悬空岛）。`Noises.seedAll` 用世界种子确定性播种。
- **GeoGenesisGenerator 接入**：`ensureTerrain(RandomState)` 首次用 `randomState.getOrCreateRandomFactory(id).fromHashOf("terrain").nextLong()` 派生种子构建 TerrainNoise；`fillFromNoise` 填充石/草/泥/沙 + 海平面以下注水；`getBaseColumn`/`getBaseHeight`/`getSpawnHeight` 同步真实高度。
- **GeoGenesisConfig 扩展 P1**：`terrainAmplitude`/`continentFrequency`(0.0006)/`mountainFrequency`(0.0016)/`detailFrequency`(0.008)/`terrainWarp`(220) 五个 DoubleValue。
- 关键坑（已沉淀 MEMORY.md）：① `Codec.dispatch` 是实例方法、type 固定为 String，必须用 `Codec.STRING.dispatch(...)`；② 节点 codec 用 `Noises.DIRECT_CODEC` 反向引用，靠字段初始化顺序打破静态初始化循环（但 Noises 静态块读节点 CODEC 会得到 null 存进 REGISTRY，P1 不解码故无害，P9 前需改）。③ `Map` 命名与 `java.util.Map` 冲突，Noises 中用全限定名。
- 下一步：**P2（Cell + Tile + 大陆）**——TerrainNoise 为临时高度源，将在 P2 被 Cell 管道取代。

## P1 运行时修复：地形切块/垂直崖壁

用户截图反馈 P1 地形呈切块（像切蛋糕），垂直崖壁明显。根因：`Seed.at()` 第二次置换表索引用 `&255` 截断，但表长 512，正确应 `&511`；导致噪声周期错位，256 方块边界出现断裂。已修复并重新 build 通过（EXITCODE=0）。用户需重新跑 `runClient --offline` 验证。

## P2 实现完成（07:52，build 通过 EXITCODE=0）

用户确认「两者都要（接 Generator + 独立预览）+ 切到 Cell 管线，TerrainNoise 退场」。已交付：
- **零 MC 依赖地形引擎**（包 `worldgen/terrain/`）：`Cell`/`Size`(cell 为单位,16/HS 换算)/`Continent`+`SimpleContinent`(低频+Warp, R1)/`HeightCurve`(分带噪声→Y)/`BasicClimate`(纬度温度+海岸湿度雏形)/`CellGenerator`(applyContinent→applyHeight→applyClimate 管道)/`GeoGenesisTerrain`(唯一入口 sampleHeight/getChunkCells/getRegionCells)/`Tile`+`TileCache`(LRU+TTL 同步)。
- **Generator 切换到 GeoGenesisTerrain**：`GeoGenesisGenerator.ensureTerrain` 派生种子 → `new GeoGenesisTerrain(seed, Size.of(HS), configParams())`；`TerrainNoise.java` 已删除。
- **独立预览窗口** `client/preview/TerrainPreview.java`（Swing 拖拽/滚轮/高度热力图）+ Gradle `runPreview` 任务（JavaExec, classpath=sourceSets.main.runtimeClasspath, **不启动 MC**）。默认 scale=4 可见大陆尺度。
- **形状公式与 P1 逐字一致**（`0.25*c + 0.2*amp*(m*landMask) + 0.03*d`，`landMask=clamp((c+0.2)/0.65,0,1)`），保证观感延续，仅最终映射换成分带 HeightCurve。
- **无头自检验证**（临时 TerrainSanity 跑完已删）：默认地形 陆地~52%/海洋~48%/山地~7.2%/雪峰~0.2%，高度范围 [27,113]，海岸线/丘陵/山脉分层正常。
- 关键标定：① `HeightCurve` 阈值按真实 e∈[-1,0.375] 重新标定（BEACH 0.02/HILL 0.08/MTN 0.18/PEAK 0.28），原 PLAN 阈值(山0.55/峰0.80)永不触发；② 大陆波长≈1667 方块，采样窗口<2000 方块看不到海陆变化（自检曾误报 98.7% 陆地）；③ 雪线默认 140 > 最大高度~113 → 当前无雪峰（后续需调大山脉权重或降雪线）。
- 下一步：**P3（rivermap 程序化河网）** → riverDistance/valleyWidth + 主河谷雕刻。

## P2 地形现实化修复（13:xx，runSanity 验证通过 BUILD SUCCESSFUL）

用户反馈「之前说山太高是因为几乎都是高山，这不合理、不符合现实」——实际诊断：**不是山本身太高，而是大陆项 `0.25*c` 把整片陆地统一抬升**，导致陆地全浮在 63–126 窄带、无近海平面低平原做底盘，视觉上整片像连绵高地。参考 `参考/sources/ReTerraForged` 的现实化原理（地形按类别分层选高度：平原/低地只占很小高度比例贴海平面，山地是独立、由 mask 选 zones、稀有成带的高耸类别）。
- 已改 `CellGenerator.populate`：海洋模型不变（`0.25*c+0.03*d`，延续观感）；**陆地改为分层公式**——`base=0.04*landMask`(平原从海岸抬升到低矮台地) + `roll=0.06*detailLow*lm`(低频温和起伏) + `hill=0.08*(0.5+0.5*hillNoise)*lm`(丘陵，小幅度) + `mContrib=mGate*mRelief*0.95*lm*amp`(山脉，由低频 `mountainMask` 选 zones、ridged 做起伏、`mGate=((mZone-0.50)/0.50)^2` 锐化成带稀有)。`landMask=smooth(clamp(c/0.30))` 使平原贴海平面、海岸线连续、山脉只在内陆出现。
- 新增 `TerrainParams.mountainMaskFrequency`(默认 0.0009) + `GeoGenesisConfig.MOUNTAIN_MASK_FREQUENCY`(config 注入)，选「山脉在哪成带」。
- `HeightCurve.PEAK_THRESHOLD` 由 0.28 提到 0.45（雪峰只覆盖高耸山脉顶部，对应 y≈140 雪线）。
- 验证分布（seed=12345, 8000×8000）：ocean 47.4% / land 52.6%；LAND avgY=71.2, minY=63, **maxY=222**；分类 beach 23.6% / flat 60.6% / hill 32.9% / mountain 4.6% / peak 1.9%。约 93% 平原+丘陵、6.5% 成带山地、偶有 222 雪峰 → 现实化达成。
- 注：编译时的 `[removal]` 警告（FMLJavaModLoadingContext.get()、ResourceLocation(String)）是 Forge 1.20.1 自带弃用警告，非本次引入、非错误，BUILD SUCCESSFUL。
- 下一步：仍 **P3（rivermap 程序化河网）**。

## P2 地形架构升级为 TerraForged 式 region-blend（方案 B，13:4x 实施，runSanity 验证 BUILD SUCCESSFUL）
用户指出应优先参考 **TerraForged 原版**（ReTerraForged 的鼻祖），而非只看改版。据此把地形现实化从「mask 分层公式」升级为 **TerraForged 0.3.x 的 region-blend 架构**：
- 新增 `worldgen/terrain/TerrainType.java`（地形定义：name+weight 权重+landForm 高度噪声）、`LandForms.java`（plains/hills/plateau/mountains 四种 landForm，用现有 Noise 组合：`Map` 重映射 e 区间、`Terrace` 台地、`Ridge` 脊线+`CellGenerator.fractal`）、`TerrainBlender.java`（复刻原版 `TerrainBlender`：低频区域网格 + 3×3 邻域 hash 选主导地形 + 距离平方加权混合 landForm，域扭曲 warp 让边界自然）。
- `CellGenerator` 陆地分支改为 `eLand=terrainBlender.sample(x,z)` 再乘 `lm=smooth(clamp(c/0.30))` 海岸淡出；移除旧 `mountainMask/detailLow/hillNoise` 噪声；海洋模型不变。
- 地形权重（稀有度）：PLAINS 3.0 / HILLS 1.5 / PLATEAU 1.2 / MOUNTAINS 1.0 → 平原辽阔、山脉稀有成带。`TerrainParams` 新增 `regionScale`(2500)/`regionBlending`(0.55)/`regionJitter`(0.5)，`GeoGenesisConfig` 暴露 `regionScale`/`regionBlending`。
- `HeightCurve` 阈值重标定：HILL 0.30 / MTN 0.55 / PEAK 0.80（对齐地形 e 区间：plains[0,0.10]/hills[0.08,0.30]/plateau[0.30,0.55]/mountains[0.55,1.0]）；beach 改用 `continentEdge<0.03` 判真海岸（避免平原低洼误判海滩）。
- 验证（seed=12345, 12000×12000 采样）：ocean 49.3%/land 50.7%，LAND avgY≈82、minY=63、**maxY=229**，分类 beach 10.1%/flat 56.8%/hill 29.5%/mountain 3.3%/peak 0.3% → 平原+海滩 67%、丘陵 30%、高耸山地仅 3.6% 且成片稀有（雪峰冲 229），现实化达成。临时自检文件已清理。
- 下一步：**P3（rivermap 程序化河网）**。

## 修复用户截图两处视觉 bug（19:23，用户说"试试你推荐的"，BUILD SUCCESSFUL）
用户两张游戏截图反馈：① 长条状裂缝（region 边界）；② 海平面与海洋直接断开（海岸垂直断崖）。
按推荐的修复方案 A + B1 实施并验证：
- **A 海岸深度从 0 平滑起步（HeightCurve.java）**：原海洋段在 `e=0` 时 `depth=shallowDepth(20)` 直接跳深 → 垂直断崖。改为深度从 0 平滑增长：`t=-e` 归一化，`norm<0.4` 浅海段 `depth=shallowDepth*smooth(norm/0.4)`，之后过渡到 deepDepth，深海再叠海沟。`e=0 → depth=0 → y=seaLevel=63`，与陆地（`landShape(0)=0→y=63`）连续。无头自检确认：e 从 -0.06→+0.05 高度 55→67 平滑过渡，海岸 `y=63` 三值一致。
- **B1 plateau 改 soft 去 Terrace 硬台阶（LandForms.java）**：原 `plateau` 用 `Terrace(base,3,0.7)`（strength=0.7 接近硬边），在 region 混合边界暴露成直线状裂缝。改为 `band(s,freq,lo,hi)` 平滑高原（无 Terrace），移除 `Terrace` import。
- 验证：无头自检（临时 TerrainSanity 跑完已删）显示区域混合**最大邻格 e 跳变仅 0.0497**（Terrace 硬台阶时可达 ~0.25+）→ 裂缝根因消除。逐格 Y 最大跳变 24 发生在 e=0.075→0.185（丘陵带），系 HeightCurve 丘陵段控制点较陡（YS 0.02→0.28 跨度大）导致的真实陡坡，非不连续裂缝，可接受。

## 修复新世界山脉 terracing 层纹 + 方案 B 同步 PLAN.md（19:56，BUILD SUCCESSFUL）
用户确认截图是新世界，且要求把方案 B 同步进 PLAN.md。
- **山脉 terracing 根因**：`LandForms.mountains` 原用 `CellGenerator.fractal(..., ridged=true, ridgePower=2.0)`，ridged 噪声在 `|simplex|≈1` 处恒为 0 → 大片谷地卡在 e=0.55 → 固定 Y 平台 → 视觉等高线状层纹。侵蚀引擎未接入生成管线（仅 Cell.java 注释），故非侵蚀所致。
- **修复**：`mountains` 改用线性脊线 `power=1.0`（5 octave）+ 叠少量 fBm 扰动（`bumpSmall = Map(bump,-1,1,0,1)→[0,0.2]`），打破谷底平台；`Map(mixed, 0,1.2, 0.55,1.0)`。临时自检验证：e∈[0.54,0.56] 平台样本 = **0 / 400万（0.00%）**，山脉 e 范围 [0.604,0.984]，平顶平台/层纹根因消除。
- 顺手修正 `LandForms` 台地注释（原残留"梯田量化"→"低频平滑高原"）。

## 修复沿海"笔直斜线"断裂（20:3x，BUILD SUCCESSFUL）
用户反馈新世界俯视看到"笔直斜线、沿海"的真断裂（纠正我上轮"是渲染特性/方块量化"的误判）。
- **根因定位（无头自检 v2/v3/v4，临时 TerrainSanity 跑完已删）**：先排除 region 混合——6 seed×2600² 扫描单格 e 跳变 maxDE 仅 0.027~0.068，且 config 实际文件 `regionBlending=0.55`/`regionScale=2500` 与 `defaults()` 一致，高度场本质连续无真断裂。e 剖面暴露真凶：`CellGenerator.populate` 陆地分支 `lm = smooth(clamp(c/0.30))` —— lm 只在 `c∈[0,0.30]` 薄层从 1 骤降到 0，而大陆噪声在该层梯度较大（每格 c 降 ~0.017），把 eLand（可达 0.72）压成 **~15 格宽、130 格落差的沿海岸陡墙**。该墙沿 `c=0.30` 等值线（海岸内侧），俯视即"沿海笔直斜线"。
- **修复**：`lm` 过渡带扩大 `c/0.30 → c/0.75`（`CellGenerator.java`），海岸过渡拓到 ~45 格，坡度从 ~8.7 格/格 缓成 ~2.2 格/格（自然海岸崖，非断裂）。
- **附带防御加固（`TerrainBlender.java`）**：`blendRange` 加下限 `Math.max(border*blending, 0.5)`（至少覆盖半个区域周期，防 region 边界窄墙/硬断）；构造内 `blending` 钳制下限 0.30（防 config 把区域混合设成硬边界 `0=硬断`）。两者对默认参数 region 边界本就连续，属防御性。
- 验证（v3，6 seed）：海岸墙 16 格 e 差从 0.714(COAST) 降到 0.39 且最陡点移出海岸；其余 seed 降 44%~61%。残留 0.39 在 seed=777 非海岸，判为山脉内部脊线（自然蜿蜒，非直线墙）。临时自检文件 + `runSanity` task 已清理。
- **PLAN.md 同步**：在 §4「地形高度模型」末尾新增「地形生成架构（方案 B：TerraForged 式区域网格混合）」子节，记录 `TerrainType`/`LandForms`(四地形 e 区间+权重)/`TerrainBlender`(区域网格+距离平方加权混合)/`CellGenerator` 陆地分支/`TerrainParams`(regionScale/Blending/Jitter)/`GeoGenesisConfig`(REGION_SCALE/BLENDING)/HeightCurve 海岸连续行为/验证分布。临时自检文件与 `runSanity` 任务已清理。

## 山坡"等高线/梯田层纹"根因 = 方块取整，非噪声（21:5x，BUILD SUCCESSFUL）
用户多次坚称"是噪声的问题""方向错了"。本次用无头 ASCII 探针渲染真实 `sampleHeight` 验证：核心 `e` 场完全平滑有机（seed=777 平滑对角山地、seed=90210 圆形岛、seed=12345 海洋），`Simplex`/`Seed`/`NoiseUtil.hash2`/`Continent`/`BasicClimate` 全部标准无 bug；`Terrace`/`Steps`/`ErosionEngine` 均未接入 `fillFromNoise` 路径。
- **真因**：MC 1 米方块把连续 `e` 经 `HeightCurve.height()` 取整成整数 Y，在平滑低频次山坡上形成规则"等高线/梯田"台阶。这不是噪声断裂，也无法靠改山脉/海岸/region 消除。
- **上一轮 micro 修复方向对但开关装反**：`microAmp = 0.03*(1 - clamp(c/0.10))` 在海岸(c≈0)最大、内陆(c 大)归零 —— 而层纹全在**内陆山坡**，所以完全没作用 → 用户"没变化"。
- **修复（`CellGenerator.java`）**：micro 改为加在**内陆** `microAmp = 0.06*clamp(c/0.05,0,1)`（海岸压低防海滩抖动）；三 octaves（~10/5/2.5 格）细节。量化"最大等高平台长度"：内陆 micro=0(旧) → 长平台；修复后陆地最大平台降到 ~13–15 格（最平坦处，均值更短），接近原版 MC 自然陡坡。
- 验证：探针量化对比 + compileJava BUILD SUCCESSFUL。临时 `TerrainSanity` + `runSanity` task 已清理。
- **给用户的关键提醒**：① 必须重新 build 并**新建世界**实测（旧世界区块已缓存不变）；② 用户说的"笔直斜线/网络"中，自然对角线山脊、海岸线是地形本身，非 bug；若仍见规则层纹，可继续上调 `microAmp`（0.06→0.08，但过高会像月球表面）。

## 修正：Simplex 梯度表缺失轴方向 → 对角线偏置（22:3x，BUILD SUCCESSFUL）
用户贴出新图：地形变成尖刺/月球表面；并再次强调原 bug 是"**无限长笔直斜线**"，不是梯田。
- **错误**（上一条记录）：我说"核心噪声无 bug"，这是错的。`Simplex.corner()` 只生成了 4 个对角线方向梯度 `(1,1)/(-1,1)/(1,-1)/(-1,-1)`，缺少 4 个轴对齐方向 `(1,0)/(-1,0)/(0,1)/(0,-1)`。简单形网格本身沿 `x=±z` 铺排，对角梯度与网格对齐 → 沿 `x=z` 和 `x=-z` 产生**持续的山脊/谷线**，这正是用户看到的"无限长笔直斜线"。用户坚持"是噪声的问题"是对的。
- **修复（`Simplex.java`）**：用显式 8 方向梯度表 `GRAD2` 替代有 bug 的 `corner` 逻辑，并对 `hash & 7` 索引。
- **micro 回退**：0.06 三 octaves 把地形变成尖刺（用户图）。回退到 **0.02 两 octaves（~10/5 格）** 并仍保持内陆（`c` 大）生效、海岸压低。
- **验证**：
  - 对角线偏置：seed=777 沿 `x=z` 平均 Y 与 `x=z+10`/`x+10=z` 只差 ~2.3 格；seed=90210 差 <1 格。偏置基本消除。
  - 地形图：seed=777 恢复为平滑有机的山地/海岸，无尖刺。
- **给用户的关键提醒**：本次改动是全局性噪声修复（所有种子地形都会改变）；必须**clean + build + 新建世界**才能看到效果。旧世界区块仍包含旧噪声结果。临时 `TerrainSanity` + `runSanity` task 已清理。

## 移除 micro 高频细节（23:0x，BUILD SUCCESSFUL）
用户确认对角线断裂"待定"基本消失，但指出山坡"台阶"仍在；并明确说加高频噪声对侵蚀是负面影响（粒子易陷入局部侵蚀）。
- **已移除**：`CellGenerator` 中 `micro` 字段、构造、populate 内陆 micro 加成全部删除，陆地 e 恢复为 `eLand * lm` 的平滑场。
- **原因**：高频细节会制造微尺度局部起伏，侵蚀引擎（ErosionEngine 当前未接入 `fillFromNoise`）一旦接入，粒子会卡在这些局部坑里。基础高度场必须保持光滑，侵蚀才能产生真实河网/台地。
- **剩余台阶的本质**：MC 1 米方块把连续坡面取整为整数 Y，在平滑山坡上就是可见的水平台阶；不靠高频噪声无法消除。侵蚀后处理是正确解法（AGENTS.md 中 ErosionEngine 方案 9 正是为此设计，但当前未接入生成管线）。
- **下一步可选**（需用户确认）：① 接入 ErosionEngine 到 `fillFromNoise` 用侵蚀重新雕刻地形；② 用中频 domain warp（波长 > 侵蚀格尺度）把直坡扭成缓曲坡，使台阶弯曲自然；③ 直接接受台阶为 MC 方块限制。

## 台阶根因 = 陆地分支缺细节噪声（23:3x，BUILD SUCCESSFUL）
用户反驳"方块取整"说："原版噪声不会这样"——正确。查证 `CellGenerator.populate`：海洋分支有 `e = 0.25c + 0.03*d`（detail 高频），**陆地分支完全没有 detail**，陆地 `e` 只有区域混合低频 `eLand`，所以整数取整后成水平长台阶（探针量化：真实坡度区 seed=777 平均台阶 5.4 格、最长 40 格）。原版 MC 陆地高度始终叠分形细节，故取整自然。
- **修复（`CellGenerator.java`）**：新增 `landDetail = fractal(seed+71, detailFreq*7.5, 4, 2.0, 0.6)`（4 octaves，波长 ~16.7/8.3/4.2/2.1 格，gain 0.6 让短波长层有足够权重）；陆地分支 `e = eLand*lm + landDetail*0.035*lm`（海岸 lm→0 淡出不抖海滩）。
- **为何不是 0.5 gain 不够**：之前试 `gain 0.5 + amp 0.025` 只降到 4.6（最短波长层只占 6.7% 振幅，不足以在台阶上改 Y）；`gain 0.6 + amp 0.035` 后最短波长层 ≈0.69 格起伏，温和破台阶不刺突。
- **量化验证**（探针挑最陡窗口，坡度台阶=陆地连续同Y段）：seed=777(Y起伏142) 平均 1.69 / 中位 1 / p90 3 / 最长 43（中位 1 = 绝大多数相邻列高度不同，楼梯消失，与原版一致）；seed=90210(Y起伏24) 平均 3.35；seed=12345(Y起伏14) 平均 6.21（后两者低起伏/海岸区，地形本平，非楼梯）。
- **侵蚀提醒**：landDetail 最短波长 2.1 格属高频，但振幅仅 ~0.69 格，对侵蚀影响小；且 ErosionEngine 当前未接入生成管线。若日后接入侵蚀，此细节可能需重新评估（用户曾言高频噪声易使侵蚀陷入局部）。
- 探针 `TerrainSanity` + `runSanity` task 已清理。

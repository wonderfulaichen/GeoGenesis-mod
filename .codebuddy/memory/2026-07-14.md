# 2026-07-14 工作日志

## 玩家加入世界崩溃修复（biome palette null Holder NPE）

- **现象**：世界可生成（DIAG SUCCESS，生成到 97%），但玩家加入时崩溃 `Couldn't place player in world` → `NullPointerException: Holder.value() 为 null` @ `LinearPalette.getSerializedSize` → `Registry$1.getId`（即 `Registry.asHolderIdMap()` 的 `getId(Holder)`）。
- **根因**：`GeoGenesisBiomeSource.getNoiseBiome` 此前返回 `null`（`possibleBiomes` 为空集合）。`ChunkGenerator.createBiomes` → `fillBiomesFromNoise(biomeSource)` → `getNoiseBiome` 把 null `Holder<Biome>` 写进区块 biome palette，序列化时 `registry.getId(null)` 空指针。注意崩溃栈里的 `Registry$1` 是 `asHolderIdMap()` 的匿名 `IdMap<Holder<Biome>>`，说明崩溃发生在 **biome palette**（不是 block palette）。
- **修复**：
  1. `getNoiseBiome` 改为：按需初始化共享地形引擎 → `terrain.sampleCell(QuartPos.toBlock(x), QuartPos.toBlock(z))` → `BiomeClassifier.pickKey(cell)` → `ForgeRegistries.BIOMES.getHolder(key)`；地形未就绪时回退到 plains（非 null）。
  2. `collectPossibleBiomes` 改为枚举 `BiomeClassifier` 实际可能产出的全部 `ResourceKey<Biome>`（经 `ForgeRegistries.BIOMES.getHolder` 解析），`getBiomesWithin` 返回 `possibleBiomes()`。
  3. `GeoGenesisGenerator` 抽取静态 `getOrInitTerrain()`（共享引擎，`worldSeed` 播种）；`setWorldSeed` 重置 `sharedTerrain=null` 以便换种子重建。背景：ChunkStatus.BIOMES 在 fillFromNoise 之前采样 biome，故引擎必须能独立于 fillFromNoise 初始化。
- **关键 Forge 陷阱（新）**：本 Forge 1.20.1 MDK 的 `BuiltInRegistries` **没有 `BIOME` 字段**（群系是动态/Forge 注册表，已从 BuiltInRegistries 移除）。按 `ResourceKey<Biome>` 解析 `Holder<Biome>` 必须用 `net.minecraftforge.registries.ForgeRegistries.BIOMES.getHolder(key)`。同理 `BLOCK` 等 vanilla 静态注册表在 Forge 下多被标 `@Deprecated` 并指向 `ForgeRegistries`。
- 编译通过（BUILD SUCCESSFUL）。
- 待目检：runClient 进世界看 biome 是否正确按地形/气候分布（此前为 null 回退，现接真实分类）。

## 待办（阶段 4/5 仍 pending）
- mixer 重绑 + River/Erosion 进 Forge Config 6 处同步。
- runClient/runPreview 目检 + 文档终校。
- 植被装饰恢复（用户要求后续再做）。

## 纠正：ForgeRegistries.BIOMES 同步时机 → ServerLifecycleHooks 方案

- 初版修复用 `ForgeRegistries.BIOMES.getHolder(key)` 又崩溃（"minecraft:plains not registered"@ createLevels → createBiomes）。
- **根因**：`ForgeRegistries.BIOMES` 是 Forge 的静态注册表包装，在世界生成初期（`createLevels`→`createBiomes`）尚未从当前世界的 dynamic 注册表同步数据，故 `getHolder(plains)` 返回 empty → 抛 `IllegalStateException`。
- **最终方案**：优先使用 `ServerLifecycleHooks.getCurrentServer().registryAccess().registryOrThrow(Registries.BIOME).getHolder(key)`。Forge 在 `MinecraftServer` 构造期即 `setCurrentServer(this)`，早于 `runServer`→`initServer`→`createLevels`→`createBiomes`，故此时 RegistryAccess 已含完整 dynamic biome 注册表。
- 首次成功解析时缓存 `Registry<Biome>` 到静态字段，后续调用复用（稳健）。
- 兜底：无服务器时仍试 `ForgeRegistries.BIOMES`（客户端/晚于同步的路径）。
- runClient 目检（待做）。

## 创建世界卡死在 0%：ConcurrentHashMap 死锁 + 竞态

- **现象**：19 个 Worker 线程各自打印 "terrain engine initialized" 后日志终止，无 crash 但进程冻结。
- **根因 1（致命）**：`GeoGenesisTerrain.generateChunk` 末尾 `if (cache.size() > CACHE_SIZE) { cache.keySet().iterator().remove(); }`——该函数是 `cache.computeIfAbsent(key, k -> generateChunk(...))` 的 lambda。ConcurrentHashMap 的 `computeIfAbsent` 禁止在映射函数内修改同一 map（会死锁）。即使 cache 未超 256 不实际执行删除，但该代码路径仍在 computeIfAbsent 保留锁期间被扫描，多线程竞争时可死锁。
- **修复 1**：移除 generateChunk 内的 LRU 淘汰，抽为独立的 `pruneIfNeeded()` 在 `getChunkCells` 的 computeIfAbsent 外部调用。
- **根因 2**：`getOrInitTerrain()` 用 `if (sharedTerrain == null)` 竞态检查（volatile 不足原子化），19 线程同时进入 → 19 次重复初始化。
- **修复 2**：改为双重检查锁定 `synchronized(GeoGenesisGenerator.class)` 确保只创建一个共享引擎。
- 编译通过（BUILD SUCCESSFUL）。

## 设计决策：大陆性 c 区间 [0,1]→[-1,1]（2026-07-14 用户拍板）

- **理由**：MC 原版 `Continentalness` 密度函数输出 [-1,1]（负=海洋、正=陆地、0=海岸锚点）。群系模组（TerraBlender/BYG/BOP 等）均按此区间判断，对齐原版保证兼容。
- **当前状态**：代码仍用 [0,1]（`ContinentField.sample` 做 (raw+1)*0.5 归一化），设计文档已全面更新（`terrain-rebuild.md` §1.5 新增完整参数映射表 + 伪代码）。
- **需代码改动的文件**：
  1. `ContinentField.sample` — 移除以 0.5 为基的归一化，直接返回 raw∈[-1,1]
  2. `HeightCurve` 样条控制点 — 4 个 location 从 [0,1]→[-1,1]（x=2x-1）
  3. `CellGenerator.classify` — 海洋/陆地条件从 `e<0` 保持不变，但 BEACH 阈值 `e<0.03` 需重新评估
  4. `TerrainParams.defaults()` — continentThreshold 0.5→0.0；4 个 ocean loc 映射
  5. `GeoGenesisConfig` — 同步默认值；`run/config/geogenesis-common.toml` — 同步
  6. mixer `FactorCategoryBar` — c 轴范围 [0,1]→[-1,1]
- **参数映射表（已录入文档）**：deepOceanLoc 0.10→-0.80, shelfLoc 0.25→-0.50, shallowLoc 0.42→-0.16, coastLoc 0.48→-0.04, continentThreshold 0.5→0.0, continentBias 默认保持 0.0（但量程大一倍）

## runPreview 目检：海陆比异常 + 陆地全 Mountains

- 用户截图反馈：海洋~20%、陆地~80%、几乎全是 MOUNTAINS（棕色），PLAIN/HILLS/PLATEAU 几乎为零
- **根因分析**：
  - A) 海陆比：coastLoc=0.48（c-space [0,1]）+ eLand≥0.01 → 海岸线外推，更多格子判为陆地
  - B) 陆地全 Mountains：`classify` 第三行 `e>0.60 && wBelt>0.3` 过于宽松，70% 陆地命中 → MOUNTAINS；PLAIN/HILLS 判断在 MOUNTAINS 之后且阈值偏低；PLATEAU `eLand>0.5` 与 platTop=0.35 不匹配（永远不命中）
- **修正方案 A（推荐）**：重排 classify 顺序（PLAIN/HILLS 优先）+ 调低 MOUNTAINS 阈值 `e>0.75` + PLATEAU 改 `eLand>0.35`
- **待用户确认方案后执行**（先案后码原则）

## A1+A2 实施：c 区间 [-1,1] 代码切换 + classify 重排（2026-07-14 执行）

### A1：c 区间 [0,1]→[-1,1]（6 文件）

| 文件 | 改动 |
|------|------|
| `ContinentField.java` | `sample()` 移除 `(raw+1)*0.5` clamp，直接 `return root.compute(wx,wz)`；注释更新 |
| `CellGenerator.java` | `sample()` 中 `cBiased` 不再 `clamp(..., 0,1)`，改为 `cBiased = c - continentBias`；注释更新 |
| `Cell.java` | 注释 `c∈[0,1]`→`c∈[-1,1]` |
| `HeightCurve.java` | 注释 `c∈[0,1]`→`c∈[-1,1]` |
| `TerrainParams.java` | `continentThreshold` 0.5→0.0；4 oceanLoc: 0.10/0.25/0.42/0.48→-0.80/-0.50/-0.16/-0.04；注释同步 |
| `GeoGenesisConfig.java` | builder 中 continentThreshold 默认 0.5→0.0 范围 [0,1]→[-1,1]；4 oceanLoc 默认值+范围同步 [-1,1] |
| `run/config/geogenesis-common.toml` | 同步 5 个默认值 |

### A2：classify 重排判断顺序（1 文件 `CellGenerator.classify`）

- PLAIN/HILLS 提到 MOUNTAINS 之前（最常见类型不判山）
- MOUNTAINS 阈值 `e>0.60→e>0.70`，wBelt 门控 `0.3→0.35`
- PEAK 阈值 `e>0.85→e>0.80`，wBelt 门控 `0.4→0.5`
- SNOW 阈值 `e>0.90` 不变，wBelt 门控 `0.45→0.6`
- PLATEAU 独立门控 `wPlateau>0.35 && eLand>0.30`（旧 `wPlateau>0.3 && eLand>0.5` 永远不命中）
- 兜底 `return HILLS` 而非 MOUNTAINS

### 编译结果
- BUILD SUCCESSFUL（全量重编译通过）
- runPreview 目检待做

## 第二次 Biome palette id=-1 崩溃修复（2026-07-14 执行）

### 现象
- 进入世界后无方块（只看到天和云），日志 `IllegalArgumentException: No value with id -1` @ `LinearPalette.read → LevelChunkSection.read`
- 第一次进世界正常退出（chunk 没发全），第二次必炸

### 根因
1.20.1 `LevelChunkSection` 持 `PalettedContainer<Holder<Biome>>`（biome palette，不是 blocks）
`GeoGenesisBiomeSource.resolveBiome` 用 `static Registry<Biome> biomeRegistry` 缓存了第一次 server 的 Registry<Biome> 引用
第一次 world 退出 → server 关闭 → 旧 Registry 资源释放（`getHolder()` 返 empty）
第二次 world 启动 → `biomeRegistry` 仍指死 Registry → `resolveBiome(PLAINS)` 返 null → `fallback` 缓存为 null
每次 `getNoiseBiome` 返 null → biome palette 序列化 id=-1 → 客户端解码 byIdOrThrow(-1) 非法

### 修复（GeoGenesisBiomeSource.java）
- 删除 `static biomeRegistry` 缓存字段
- `resolveBiome` 改为每次实时 `ServerLifecycleHooks.getCurrentServer().registryAccess()` 解析
- `fallbackBiome()` DCL 解析 + 强制非 null（null 时抛异常保护）
- 移除不可用的 `BuiltInRegistries.BIOME` import（MC 1.20.1 Forge 中不存在该字段）
- 编译通过（BUILD SUCCESSFUL）
- runClient 目检待做

## 游戏 vs 预览海陆比不一致：toml 陆地形态参数未同步（2026-07-14 修复）

- **现象**：runPreview 海陆比 ~50/50，runClient 游戏里仍是旧占比（海~20%、陆~80%）
- **根因**：A1 只同步了 5 个 ocean/continent 默认值到 toml，遗漏了陆地形态参数（7 个 out of sync）
- **差异**：beltPeak(0.95→0.60)、plateauBase(0.55→0.20)、plateauTop(0.72→0.35)、hillsLow(0.10→0.07)、hillsHigh(0.25→0.20)、beltFoothill(0.15→0.10)、plainRough(0.04→0.03)、plateauSteps(3→4)、plateauStepStrength(0.6→0.7)
- **已修复**：toml 同步到 defaults() 值。runClient 目检待做。

## 整理 .codebuddy/plans 文件夹（2026-07-14 执行）

### 整理目标
用户要求整理 `.codebuddy/plans` 文件夹，为换新对话做准备。目标：按主题分类到子目录，删除重复/过时文件，更新索引。

### 执行内容
1. **创建分类结构**：已有 `biome/`、`fix/`、`meta/`、`preview/`、`river/`、`terrain/` 子目录
2. **移动海岸/海洋相关文件**：将根目录15个海岸/海洋相关文件移入 `fix/coast/` 子目录
3. **移动地形相关文件**：将根目录4个地形相关文件移入 `terrain/` 子目录
4. **删除重复文件**：
   - `海岸平台修复-精简方案_f1aee32b(未完成).md`（与 `海岸平台修复-精简方案_f1aee32b.md` 重复）
   - `hydraulic-erosion-parallel-fragment-fix_3f9aa345(未完成).md`（与 `hydraulic-erosion-fix-v2_054ae2ae(未完成).md` 高度重叠）
5. **更新 INDEX.md**：
   - 更新 `terrain/` 文件数：2→6
   - 更新 `fix/` 文件数：4→22（含 `coast/` 子目录19个文件）
   - 更新文件明细部分，添加新移动的文件
   - 删除重叠文件条目

### 整理后结构
```
.codebuddy/plans/
├── biome/          # 群系分类重构（3个文件）
├── fix/            # Bug修复 + 侵蚀修复（3个直接文件 + coast/子目录19个文件）
├── meta/           # 元计划（2个文件）
├── preview/        # 预览增强（4个文件）
├── river/          # 河流相关（droplet/ + explore/）
├── terrain/        # 地形重建（6个文件）
└── INDEX.md        # 更新后的索引
```

### 验证
- 所有计划文件已分类到子目录，根目录仅剩 `INDEX.md`
- `INDEX.md` 已更新，反映当前组织状态
- 重复文件已删除，减少混乱

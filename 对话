河流系统设计目标
基于已有的基础地形，自动生成树状河网（主流+支流）。

河流具有自然的蜿蜒形态、合理的宽度与深度变化。

保证无限世界无缝、完全确定性（由世界种子决定）。

与后续侵蚀管线兼容（河流本身可作为一种“预设沟壑”引导侵蚀）。

1. 总体流程
text
基础高度图 → 流向计算 → 流量累积 → 河流网络提取 → 河道雕刻 → 宽度/深度分配 → 与地形融合
所有步骤均在区块生成时按需实时计算，无需预生成全局高度图。

2. 核心算法步骤
2.1 流向计算（连续梯度方向）
禁止使用 D8 离散方向，采用双三次插值 + 连续梯度：

对任意世界坐标 (x, z)，用双三次插值从基础高度场（包含大陆、山脉、细节噪声）获取高度 h。

计算梯度 (∂h/∂x, ∂h/∂z) 通过中心差分或解析导数。

流向方向 = 梯度向量的反方向（水往低处流）。

使用浮点坐标追踪流线，步长 0.3~0.5 方块。

2.2 流量累积（稀疏采样 + 局部流线）
为了性能，不计算每一个方块，而是采用种子点流线反向追踪法：

以区块为单位，在每个区块中随机撒 N 个“雨滴”种子点（N 由区块面积和密度参数决定，如每 4×4 方块一个点）。

对每个种子点，沿流向方向向前追踪（向下游移动），直到：

遇到海洋（高度 ≤ 0），

遇到已存在的“河流像素”，

或追踪步数超过上限（如 5000 步）。

追踪路径上经过的所有像素，其累积流量 +1（或+该水滴的初始水量）。

确定性要求：种子点位置和追踪步中的任何随机扰动必须由世界种子和区块坐标派生。

2.3 河流网络提取
对累积流量图进行阈值处理：流量 > riverThreshold 的像素标记为“河流”。

riverThreshold 控制河网密度，典型值为累积流量最大值的 0.5%~2%。

对标记像素进行细化（骨架化），确保河流线宽度为 1 像素，便于后续宽度分配。

2.4 河道雕刻
对于每个标记为河流的像素，计算其应有的深度和宽度，然后向高度图施加下切。

深度计算
基础下切深度 baseDepth 与 sqrt(流量) 成正比：

text
depth = minDepth + sqrt(flow / maxFlow) * (maxDepth - minDepth)
minDepth 和 maxDepth 可控，如 1.0 和 8.0。

深度随河流级别增加（主流更深）。

宽度计算
宽度同样与 sqrt(流量) 成正比，并受局部岩层硬度影响。

河床剖面形状：使用余弦或线性衰减，向两岸逐渐变浅。

对河流像素及其垂直方向上的邻域像素进行下切：

text
for d in [-halfWidth, halfWidth]:
    factor = cos(π * d / (2 * halfWidth))  // 河床平滑剖面
    height[x+dx*d][z+dz*d] -= depth * factor
其中 (dx, dz) 是垂直流向的单位向量。

2.5 蜿蜒（蛇曲）效果
为了自然蜿蜒，在追踪流线时加入曲率驱动的侧向侵蚀或直接使用噪声扭曲：

在流线追踪坐标上叠加低频噪声扰动：

text
flowX = x + sin(noise(x * freq, z * freq) * amplitude
flowZ = z + cos(noise(x * freq, z * freq) * amplitude
或者使用更真实的“流线曲率推动河道横向迁移”的解析模型：河道曲率大的地方，外侧被侵蚀、内侧沉积。这需要计算流线的二阶导数。简单起见，噪声扰动即可获得良好视觉效果。

2.6 湖泊与宽谷
局部洼地（无下游出口或流量汇聚极大）自动形成湖泊。

检测：如果流线追踪进入一个局部极小值区域且无法流出，则标记该洼地为湖泊，用平面高度填充（湖泊水面高度 = 洼地溢出点高度）。

湖泊深度由洼地深度决定，湖底可平整或保留微地形。

3. 无限世界无缝实现
所有操作基于世界绝对坐标，不存储全局纹理。

流量累积使用区块局部缓存：对于每个区块，我们只需要知道该区块及其周围 1~2 区块的流量状态。

使用确定性种子点追踪：当区块被请求时，我们生成该区块内的雨滴种子点，并向前追踪流线。如果流线进入未生成的区块，可以提前生成该区块的基础高度（不需完全生成，只提供高度值），或使用预测高度函数（基础噪声可直接查询）。

河流网络的骨架化可直接在流量累积后按区块逐块处理，相邻区块边界通过重叠查询保证河流线连续。

河道雕刻时，若河流像素靠近区块边缘，查询相邻区块的高度值（通过基础地形函数），确保下切深度平滑过渡。

4. 与侵蚀管线的整合
河流系统应作为预侵蚀沟壑网络嵌入多级管线：

在基础地形生成后、宏观侵蚀之前，先运行河流生成并下切河道。

这样宏观侵蚀粒子可以沿着这些河道流动，进一步加深和拓宽它们，使侵蚀结果与河流网络一致。

河流下切后的高度图作为侵蚀算法的输入，同时为侵蚀粒子提供优先路径（例如在河道内增加侵蚀速率）。

5. 关键参数（建议可调）
参数	默认值	说明
rainDensity	0.0625 (每4×4一个点)	控制河网密度
riverThreshold	0.01 (相对最大流量)	多大流量开始形成河流
minDepth	1.0	最小河流深度
maxDepth	8.0	最大河流深度
widthMultiplier	0.3	宽度缩放因子
meanderAmplitude	15.0	蜿蜒强度
meanderFrequency	0.01	蜿蜒频率
lakeMaxDepth	10.0	湖泊最大深度